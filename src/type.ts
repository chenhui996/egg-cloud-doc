// ------------------------------------------------------------------------------------------------------------------------

import {KeyboardEvent} from 'react';

// 搜索框组件
export interface FileSearchProps {
    onFileSearch: (val: string) => void;
}